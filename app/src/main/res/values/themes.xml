<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="BaseDialogTheme" parent="android:Theme.Holo.Light.Dialog.NoActionBar">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="MenuDialogTheme" parent="BaseDialogTheme">
        <item name="android:windowMinWidthMajor">25%</item>
        <item name="android:windowMinWidthMinor">25%</item>
        <item name="android:background">@drawable/bg_dialog_help</item>
        <item name="android:windowBackground">@drawable/bg_dialog_help</item>
    </style>
</resources>